on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    uses: carlthome/workflows/.github/workflows/python.yaml@main
    with:
      package-name: audioscrape
